spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:116.198.226.68}:${DB_PORT:3306}/${DB_NAME:finance_db}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:root2026!@#}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1

jwt:
  secret: ${JWT_SECRET:test-jwt-secret-key-for-unit-testing-at-least-32-chars}
  expiration: 604800000

logging:
  level:
    root: WARN
    com.xingzhewk: WARN
